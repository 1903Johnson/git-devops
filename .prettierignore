pnpm-lock.yaml
node_modules
dist
coverage

# Markdown is deliberately excluded: Prettier reflows tables and wrapped prose, which
# produces large diffs in the docs both agents read and edit. Content review catches
# formatting problems there; a formatter does not need to.
*.md
